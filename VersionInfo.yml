variables:
  GROUP_NAME: SdgGames
  GAME_NAME: Pac_Man

  # Major.Stable.Unstable
  # Major release is a public game release or re-launch
  # Stable is used for any public update that is not a game launch
  # Unstable is incremented for every frequent (biweekly?) release
  RELEASE_NUMBER: "0.1.0"
  GODOT_VERSION: "3.3.4"
